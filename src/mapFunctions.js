/*
  mapFunctions.js

  This object is provided so that the implementation team can supply their own
  "map" function if required.
*/
const mapFunctions = {
  identity: x => x
}

module.exports = mapFunctions;